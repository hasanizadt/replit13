import { Product } from './product.model';
import { Brand, BrandMeta, GetBrands } from '../../brand/models/brand.model';

// Using Brand, BrandMeta, and GetBrands from brand module instead of local definition
export { Brand, BrandMeta, GetBrands };